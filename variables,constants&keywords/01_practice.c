#include<stdio.h>

int main(){
    int length,breadth,area;
    printf("enter the length of a rectangle");
    scanf("%d", &length);
    printf("enter the breadth of a recbreadth");
    scanf("%d", &breadth);
    printf("area of rectangle is  %d", area=length*breadth);
    return 0;
}